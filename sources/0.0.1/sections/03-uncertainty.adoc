
[[sec-data-product-identification]]
== Measurement Uncertainty in Hydrography

=== Introduction

* Uncertainty is used to support our understanding of the measurements (start with 3 examples). It's about managing uncertainties, because some aspects can be controlled.
* The importance of an uncertainty budget (prediction) and estimating the uncertainty from the data (data-driven).
* Contrary to land surveying techniques, the establishment of control points is mostly not feasible in hydrographic surveying. Hence, we cannot use the same metrics to evaluate accuracy of e.g. depth, current speed. Principles are the same but evaluation tools are not the same. Often, the error budgets will be smaller on land versus sea because we can better control the measurement factors.
* Accuracy should not be confused with uncertainty. Based on the (new) definition of S-32, Accuracy should be qualitative and be related to bias.

S-44 Edition 5 (IHO, 1998) advocated the general adoption of the term _uncertainty_ instead of _error_ in order to better align with the Guide to the Expression of Uncertainty in Measurement (GUM, 2008). This was reflected for example by the change from _Total Propagated Error_ (TPE) to _Total Progagated Uncertainty_ (TPU). The implications of this adaption are however greater than just a change in terminology. Rather, they warrant a change from a more traditional approach to error estimation (i.e. the Error Approach), in which a clear distinguish is made between random and systematic error estimation. In the recommended Uncertainty Approach, this distinction is not made as the nature of the error may not be subject to categorization. Indeed, an error may, for example, display a random nature in the short-term and a systematic nature in the long-term or vice-versa. Instead, the Uncertainty Approach attempts to assign an interval in which the unknowable true value of a measurement is expected to lie as defined within a particular confidence level, irrespective of the nature of that error (random or systematic).

The rest of this chapter is structured as follows. Section 2 will further describe concepts related to error theory as they should be applied to hydrographic surveying. Section 3 will describe how uncertainty should be applied in practice in hydrographic surveying. More emphasis is placed on depth measurement uncertainty, but it should be recognized that measurement uncertainty also applies to other hydrographic measurand such as surface current and tides.

=== Fundamentals of Error Theory

All measurements carry error; the “truth” is unknowable and only approached probabilistically. In hydrography this matters directly to safety of navigation and downstream products (ENCs, S-102 surfaces, contours, shoal-biased selections). Rather than treating a single accuracy number as definitive, modern survey practice expresses how far a measured value may be from the true value with a stated confidence level. This section sets error-theory foundations and then frames how we quantify and combine uncertainties for hydrographic use.

==== Concept of Error

*Error* is the difference between a measured value and the (unknown) true value. Two behaviors are distinguished:

* *Systematic error (bias)*: repeatable offset or trend (e.g., sound-speed model bias, tide/ellipsoid separation bias, GNSS antenna height mis-modeling). Predictable once detected; ideally corrected. Residual bias contributes to total error.
* *Random error (noise)*: zero-mean scatter from unmodeled variability (e.g., beam steering jitter, heave filtering residuals, electronic/photon noise, short-scale water-level variability). Averages down with repetition.

*Blunders* (gross mistakes, e.g., wrong lever-arm sign, wrong epoch) are not treated as “error”; they are data-quality failures to detect and remove via Quality Check.

==== Accuracy and Precision in Hydrography
:stem:

*Accuracy / bias*: proximity of the measured quantity to a reference or true value. Estimated via comparisons to control (reference surfaces, crosslines, junctions, check lines, control points) and by tracking known bias contributors (vertical datum realization, sound speed, latency).

*Precision / variance (standard deviation)*: repeatability under unchanged conditions. For depth, summarize precision as a standard deviation, stem:[\sigma_{z}]; for position as stem:[\sigma_{x}, \sigma_{y}] (often summarized radially as stem:[\sigma_{p}]).

Hydrographic standards express Total Propagated Uncertainty (TPU) with:

* TVU (Total Vertical Uncertainty) for depth, at a stated confidence level.

* THU (Total Horizontal Uncertainty) for position, likewise at a stated confidence.
Bias (systematic) and variance (random) both load into TVU/THU.

==== Root Mean Square Error (RMSE)

RMSE is used when comparing surveyed values to independent references because it captures both random scatter and any persistent offset in one metric:

[stem]
++++
RMSE = \sqrt{\frac{1}{N} \sum_{i=1}^{N} (z_{i}^{survey} - z_{i}^{ref})}
++++

If a mean offset stem:[\overline{\Delta}] is removed first, the residual RMSE approximates random uncertainty; if not, RMSE reflects both random and systematic components:

[stem]
++++
RMSE^{2} \approx \sigma^{2} + \overline{Delta}^{2}
++++


Good practice:

* Ensure independence and comparable support (same vertical datum, similar spatial resolution, co-registration).
* State whether a bias term was removed and report the bias separately.

==== Why Hydrography Uses an Uncertainty Approach

Single “accuracy” numbers cannot answer the operational question: How far from truth might this depth/position be? Survey systems blend many contributors (GNSS/INS, timing, sound speed, beam geometry, refraction, tides/vertical separation, gridding/generalization). Some vary in time/space; others are persistent. An uncertainty framework:

* makes contributors explicit,
* combines them into TVU/THU at a stated confidence level,
* supports survey design (a-priori predictions) and Quality Analysis (a-posteriori checks),
* and communicates fitness for use to end-users (e.g., CATZOC/QoBD, S-102 uncertainty layers).

==== Uncertainty Framework in Hydrography

===== Key Concepts

* *Uncertainty*: non-negative estimate characterizing the range within which the true value lies, given a confidence level.

* *Assumed error distribution*: depth errors are commonly modeled as normal (Gaussian). Under this assumption:

** 1D (depth): stem:[95% \approx 1.96 \times \sigma_{z}]
** 2D (position): stem:[95% \approx 2.45 \times \max(\sigma_{x}, \sigma_{y})] if axis-aligned summaries are used (or compute from full covariance when available).

* *Confidence level (95%)*: always state the level with the uncertainty value.

===== Estimating Uncertainty (two complementary practices)

. From data variability (repeats and internal consistency):

* Within-track repeat pings (after outlier control).
* Crosslines and line-to-line differences (junction analysis; remove trends before statistics).
* Surface differences against an independent reference (check survey, reference area).
* #Note: common-mode effects (e.g., datum shift, sound-speed bias) cancel and will not appear—so this is typically a lower bound unless bias is separately estimated.#

. From models, calibration, and specifications (propagation):

Build a contributor budget and propagate through the geolocation/measurement model: sensor specs (range noise, beam angle), timing/latency, GNSS/INS covariance, lever arms/boresight, heave, sound-speed profile uncertainty and ray-path modeling, tide/vertical separation model uncertainty, refraction, and gridding/generalization.
Use analytical propagation (Jacobians) where linearization is adequate; use Monte Carlo where nonlinearity is strong (e.g., refraction, bathymetric inversion).

*Best practice*: predict a-priori to design and monitor; verify a-posteriori against repeats, crosslines, and reference areas; estimate and report any residual bias.

===== Combining Uncertainties

For independent contributors stem:[\mu_{1}, ..., \mu_{k}] (as standard uncertainties, 1σ), combine in quadrature:

[stem]
++++
\mu_{combined} = \sqrt{\sum_{j=1}^{k} \mu_{j}^{2}}
++++

Then convert to the reporting confidence level (e.g., stem:[\times 1.96] for 95% in 1D). For correlated terms, include covariances or use Monte Carlo.

Implementation notes:

* TVU: combine vertical contributors (include residual bias if uncorrected).

* THU: combine horizontal contributors (GNSS/INS, timing, lever arms/boresight, beam steering, etc.).

* Surfaces vs soundings: if delivering a gridded surface, report the uncertainty of the cell estimate (not of a single ping); account for spatial averaging and resolution.

===== Hydrographic Examples

* MBES surface with variable sound speed: THU often dominated by GNSS/INS; TVU mixes range noise, beam-angle effects (growing off-nadir), and refraction from SSP uncertainty. Mitigate via frequent SVP/CTD, real-time ray tracing, strong crossline regime; include vertical datum realization terms.
* Topo-bathy LIDAR: TVU comprises subaerial (platform/trajectory/geolocation) and subaqueous components (surface refraction, optical properties). The subaqueous part is nonlinear; Monte Carlo is preferred; asymmetric error bounds can appear. Include vertical datum transfer uncertainty.
* SDB (empirical/ML/RTE): Depth is derived. External uncertainty estimation via high-quality MBES reference and held-out validation; full propagation (sensor radiometry, water-leaving radiance model, IOPs, adjacency, water level) for predictive bounds. Site transferability is limited; re-validation is required.

===== Summary

* Every hydrographic depth/position value should be accompanied by an uncertainty at a stated confidence level. TVU/THU encapsulate both random scatter and residual bias.
* RMSE is useful for external checks; disclose and separately report any bias removed.
* Plan (a-priori), monitor, and verify (a-posteriori); document contributors and assumptions.
* Preserve/propagate uncertainty in deliverables (e.g., BAG node uncertainties, S-102 metadata); ensure cartographic generalization does not imply better quality than the source.
* Quality indicators for end-users should reflect the processed outcome, not just instrument capability.


=== Uncertainty Applied to Hydrographic Surveying

Uncertainty is an inherent property of all measurements. For mariners, as navigation increasingly adopts explicit risk-management practices, quantified uncertainty becomes critical to decision-making—particularly where economic pressures on efficiency lead vessels to operate with reduced under-keel clearance and closer to charted depths. Consequently, surveyors are increasingly required to understand, manage, estimate, and report uncertainty.
In hydrography, uncertainty is commonly classified by (i) when it is evaluated and (ii) how it is evaluated.

1) Timing of evaluation

*A priori uncertainty*. Estimated before data acquisition, typically from manufacturer specifications, laboratory calibration results, and modelling of known error sources. Because these estimates do not use survey data, they may not reflect the specific environmental and operational conditions of the survey area.
	
*A posteriori uncertainty*. Estimated from the acquired survey data, often by aggregating one or more parameters derived from the observations (e.g., line-to-line or surface differences). These estimates generally better represent the uncertainty actually achieved during the survey.

2) Method of estimation

*Model-based approach*. Makes use of mathematical models to derive uncertainty from known sources of variance. This approach can be applied a priori for planning and acceptance criteria and then refined a posteriori with data collected, survey-specific calibrations, etc.
	
*Empirical approach*. Estimates uncertainty directly from the dispersion of observations, such as repeat lines, crosslines, beam-to-beam or line-to-line differences, surface-difference statistics, or algorithmic estimators (e.g., gridding methods that output per-node uncertainty). This approach is inherently a posteriori because it requires the collected data.

Practical considerations

Model-based estimates often adopt conservative inputs to encompass all identified error sources and therefore may overestimate the true uncertainty. Empirical estimates can underestimate uncertainty if systematic effects or unobserved biases are not captured by the available data (e.g., uncompensated offsets, spatially correlated refraction errors, unmodelled timing biases). Best practice is to use both: employ a priori TPU for planning and initial compliance with standards, then confirm and—where appropriate—refine uncertainty a posteriori. Use empirical methods to validate final estimations (e.g., crossline analysis and surface consistency assessments). Final reporting should state the basis of the uncertainty estimate (model-based vs. data-driven, or hybrid), the coverage to which it applies, and any assumptions or limitations.

==== Estimating Depth Uncertainties

Estimating depths are inherently uncertain due to a combination of instrument limitations, environmental variability, and processing methods. Understanding and quantifying these uncertainties are essential to ensuring safety of navigation, critical for producing reliable bathymetric products and instrumenting scientific- and engineering decision making. 

The predictive and data-driven approaches are fundamentally different in the sense that the predictive approach will estimate an uncertainty for each sounding measurement while the data-driven approach will estimate the uncertainty from a statistical computation from multiple sounding measurements within an appropriate area or node. Each method has distinct purposes, advantages, and limitations, and they are most effective when used together. 

The predictive approach estimates the uncertain of depth measurements based on the knowledge of system uncertainties, sensor capabilities, and environmental factors. This approach provides per-sounding uncertainty, which is beneficial during acquisition and processing. Since the approach relies on accurate characterization of each source of uncertainty, it tends to conservatively overestimate the combined uncertainty. 

The data-driven approach estimates uncertainty after depth measurements have been acquired, using statistical analysis of the measured depths. Instead of collecting each source of uncertainty, the approach relies on actual measurement dispersion to infer the reliability of depth estimates within areas or nodes. This approach directly reflects the variability of depth measurements and useful when producing bathymetric products. However, it tends to underestimate uncertainty, as it neglects systematic uncertainties which effects all depth measurements. In addition, the data-driven approach requires sufficient data density to produce valid statistical estimates of uncertainty.

===== Predictive Approach

In hydrographic surveying, understanding and minimizing TPU, TVU, and THU are crucial. These components influence the reliability of the data and, consequently, the safety and efficiency of maritime operations.  

Total Propagated Uncertainty (TPU) is a comprehensive measure used in hydrography to quantify the overall predicted uncertainty/error in three dimensions associated with the positioning and measurement of hydrographic data e.g. a sounding on the seafloor. It encompasses various sources of error that can influence the accuracy of hydrographic surveys, including sensor inaccuracies, expected environmental conditions, model inaccuracies and data processing methods. Instead of TPU, the term “error budget” is also used. TPU is particularly relevant in the context of survey preparation and execution were it can be used as a tool to asses if the specified requirements of a survey is expected to be met using a specific set-up that is intended to measure hydrographic data.

image::https://github.com/eriffon/C-13-Manual-on-Hydrography/blob/writing_chap3/sources/0.0.1/TVU.png[Propagation of Uncertainty]

#All measurements are considered to be uncorrelated random variables with normally distributed uncertainties, all uncertainty sources acting independently, so all covariance terms can be disregarded, and the TPU can be computed statistically using the well-known Law of Uncertainty Propagation.# 

The two primary components of TPU are Total Vertical Uncertainty (TVU) and Total Horizontal Uncertainty (THU). 

THU is the component of total propagated uncertainty (TPU) calculated in the horizontal dimension. THU is a two-dimensional quantity with all contributing horizontal measurement uncertainties included, whereas TVU is the component of total propagated uncertainty (TPU) calculated in the vertical dimension. TVU is a one-dimensional quantity with all contributing vertical measurement uncertainties included.  

Uncertainties can be caused by factors such as uncertainties in vessel and instrument positioning, sound velocity variations, equipment calibration errors, vessel motion, tide corrections or uncertainty in the bending of light in the water when using techniques like ALB.  

====== Mathematical Approach to Uncertainty Calculation

The calculation of THU and TVU is based on the principles outlined by Hare et al. (1995), who proposed a systematic approach to quantify uncertainties by considering all relevant sources of error. The core concept involves the quadratic sum of individual uncertainties, which is expressed mathematically as:

[stem]
++++
U_{total} = \sqrt{U_{1}^{2} + U_{2}^{2} + ... + U_{n}^{2}}
++++

In this formula, stem:[U_{1}^{2}, U_{2}^{2}, U_{n}^{2}] represent different sources of uncertainty, such as measurement errors, instrument inaccuracies, and environmental influences. This approach assumes that the errors are independent and normally distributed, which is a common assumption in error analysis.

====== Calculation of THU and TVU 

For horizontal uncertainty, the individual uncertainties in the x- and y-directions are combined using the quadratic sum, resulting in: 

[stem]
++++
THU = \sqrt{U_{x}^{2} + U_{y}^{2}}
++++

This formula indicates that the total horizontal uncertainty is derived from the combined effect of errors in both directions, providing a single measure of horizontal positional accuracy. 

Similarly, the vertical uncertainty (TVU) is calculated based on the uncertainty in the z-direction, which can be expressed as: 

[stem]
++++
TVU = U_{z}
++++

This value represents the overall vertical accuracy, considering all sources of error affecting depth measurements. 

===== Data-driven Approach

* Describe how to estimate uncertainty at a specific position/node. 
* There is no standard parameter to calculation standard uncertainty. Examples can be:
   - This can amount to either computing the standard error
   - SDASN (standard deviation along the surface normal)
   - (as proposed by Calder) to compute the variance of only the observations contributing to the depth estimate at that position/node.
* Describe how crosslines are useful at estimating the uncertainty in the (measurement) sensor frame and are therefore a sensor performance indicator. Describe how overlaps between independent survey systems may be used to estimate uncertainty (expressed as a mean square error).

==== Practical Applications of Uncertainty

===== Data Processing

* Using TPU for grid generation (e.g. CUBE)
* Using TPU as a filter in real-time or in post-processing
* See Chapter 8

====== Nautical Products

* Uncertainty can be a contribution to CATZOC
* Used as a quality indicator to disambiguate (criteria decision) between surveys for chart production
* Uncertainty can be the input to the Uncertainty Layer of S-102

==== Future Trends in Uncertainty Management

This section highlights where hydrographic uncertainty practice is heading—especially for gridded products (e.g., S-102) and derived depths (e.g., SDB)—focusing on three ML-adjacent ideas that strengthen how we express risk without changing field procedures.
Quantile Prediction (predicting uncertainty directly)
What it is. Models that learn conditional quantiles (e.g., 5th/50th/95th percentiles of depth given features), providing native prediction intervals rather than inferring them post-hoc from variance.
Why it matters. Hydrographic errors are typically heteroskedastic (grow with depth). Quantile models flex to that, returning wider intervals where the seafloor or water column are challenging.

Locally Adaptive Prediction (uncertainty that follows the neighborhood)
What it is. Methods that adapt model complexity and interval width to local feature space or geography (e.g., nearest-neighbor quantile smoothing, local bandwidth selection, geographically weighted learners).
Why it matters. Error structure changes across a block—over sand waves vs. rock outcrops; in clear vs. turbid patches. Local adaptivity yields tighter intervals in stable areas and wider bands where conditions shift.

Conformal Prediction (valid coverage, distribution-free)
What it is. A calibration wrapper around any model that produces prediction sets or intervals with formal coverage guarantees (e.g., 95%) under minimal assumptions, using a held-out calibration set.
Why it matters. Even well-tuned models can be mis-calibrated. Conformal methods re-scale residuals so that stated 95% intervals empirically contain the truth ~95% of the time—crucial for safety decisions.
